[toolchain]
channel = "1.89.0"
profile = "minimal" # Don't download docs, sources, etc. on CI
# `minimal` omits rustfmt and clippy, and both are quality gates here.
components = ["rustfmt", "clippy"]
